# 👥 دليل العمل الجماعي - DIPS Academy

## 📚 نظرة عامة

هذا المشروع مُعد للعمل الجماعي بين 3 مطورين باستخدام Git Flow.

## 🌿 بنية الفروع (Branches)

### الفروع الرئيسية:

- **`main`**: الفرع الرئيسي (الإنتاج) - يحتوي على الكود المستقر والجاهز للنشر
- **`develop`**: فرع التطوير - يتم دمج جميع الميزات الجديدة فيه قبل الدفع إلى main

### فروع المطورين:

- **`feature/developer-1`**: فرع المطور الأول
- **`feature/developer-2`**: فرع المطور الثاني
- **`feature/developer-3`**: فرع المطور الثالث

---

## 🚀 خطوات البدء للمطورين

### 1. استنساخ المشروع (Clone)

```bash
git clone https://github.com/Omar-hajjoub/DIPS-Academy.git
cd DIPS-Academy
```

### 2. تثبيت التبعيات

```bash
# تثبيت تبعيات PHP
composer install

# إنشاء ملف .env من النموذج
copy .env.example .env

# توليد مفتاح التطبيق
php artisan key:generate

# إنشاء قاعدة البيانات
New-Item -Path "database\database.sqlite" -ItemType File

# تشغيل Migrations والبيانات الأولية
php artisan migrate --seed
```

### 3. التبديل إلى فرعك الخاص

**المطور 1:**
```bash
git checkout feature/developer-1
```

**المطور 2:**
```bash
git checkout feature/developer-2
```

**المطور 3:**
```bash
git checkout feature/developer-3
```

---

## 💻 سير العمل اليومي

### 1. قبل البدء بالعمل (سحب التحديثات)

```bash
# تأكد من أنك على فرعك
git checkout feature/developer-X  # استبدل X برقمك

# سحب آخر التحديثات من فرع develop
git pull origin develop

# حل أي تعارضات (conflicts) إن وجدت
```

### 2. أثناء العمل

```bash
# إضافة التغييرات
git add .

# حفظ التغييرات (Commit)
git commit -m "وصف واضح للتغييرات"

# رفع التغييرات إلى GitHub
git push origin feature/developer-X
```

### 3. دمج عملك في فرع التطوير (Pull Request)

1. اذهب إلى: https://github.com/Omar-hajjoub/DIPS-Academy
2. اضغط على **"Pull requests"**
3. اضغط على **"New pull request"**
4. اختر:
   - **Base**: `develop`
   - **Compare**: `feature/developer-X` (فرعك)
5. اكتب وصفاً للتغييرات
6. اضغط **"Create pull request"**
7. انتظر مراجعة الفريق والموافقة

---

## 📋 قواعد العمل الجماعي

### ✅ افعل:

1. **اعمل دائماً على فرعك الخاص** - لا تعمل مباشرة على `main` أو `develop`
2. **اسحب التحديثات يومياً** من `develop` قبل البدء بالعمل
3. **اكتب رسائل commit واضحة** بالعربية أو الإنجليزية
4. **اختبر الكود قبل رفعه** - تأكد من عدم وجود أخطاء
5. **راجع Pull Requests الخاصة بزملائك** وعلق عليها

### ❌ لا تفعل:

1. **لا ترفع (Push) مباشرة إلى `main` أو `develop`**
2. **لا ترفع ملف `.env`** - هو في .gitignore
3. **لا ترفع مجلد `vendor` أو `node_modules`**
4. **لا تدمج (Merge) فرعك بنفسك** - استخدم Pull Request دائماً

---

## 🔧 الأوامر المفيدة

### التحقق من الفرع الحالي
```bash
git branch
```

### عرض التغييرات غير المحفوظة
```bash
git status
```

### عرض سجل التغييرات
```bash
git log --oneline --graph --all
```

### التراجع عن تغييرات غير محفوظة
```bash
git restore .
```

### حل التعارضات (Conflicts)
```bash
# 1. سحب التحديثات
git pull origin develop

# 2. افتح الملفات المتعارضة وعدّلها يدوياً
# 3. أضف الملفات بعد حل التعارض
git add .

# 4. أكمل الدمج
git commit -m "Resolve conflicts with develop"
```

---

## 📝 أمثلة على رسائل Commit الجيدة

```bash
git commit -m "إضافة صفحة تسجيل الدخول"
git commit -m "إصلاح خطأ في عرض الدورات"
git commit -m "تحسين أداء صفحة Dashboard"
git commit -m "Add user authentication feature"
git commit -m "Fix: Resolve database connection issue"
```

---

## 🆘 حل المشاكل الشائعة

### مشكلة: "Your branch is behind"
```bash
git pull origin feature/developer-X
```

### مشكلة: "Conflict in file"
1. افتح الملف المتعارض
2. ابحث عن علامات `<<<<<<<` و `=======` و `>>>>>>>`
3. عدّل الكود واحتفظ بالنسخة الصحيحة
4. احذف علامات التعارض
5. نفذ:
```bash
git add .
git commit -m "Resolve conflict"
git push
```

### مشكلة: رفعت commit خاطئ
```bash
# التراجع عن آخر commit (مع الاحتفاظ بالتغييرات)
git reset --soft HEAD~1

# التراجع عن آخر commit (وحذف التغييرات)
git reset --hard HEAD~1
```

---

## 🔒 الأمان

- **لا تشارك بيانات `.env` مع أحد**
- **لا تضع كلمات مرور في الكود**
- **استخدم متغيرات البيئة للبيانات الحساسة**

---

## 📞 التواصل

- استخدم **Issues** على GitHub لتوثيق المشاكل
- استخدم **Pull Request Comments** للمناقشات
- تنسق مع الفريق قبل إجراء تغييرات كبيرة

---

## 📊 معلومات المشروع

- **Laravel Version**: 11.48.0
- **PHP Version**: 8.4.17
- **Filament Version**: v3.3.47
- **Database**: SQLite

### بيانات تسجيل الدخول للتطوير:
- **Email**: superadmin@dips-academy.com
- **Password**: password

---

**ملاحظة**: إذا واجهت أي مشكلة، راجع ملف `TROUBLESHOOTING.md` أو تواصل مع الفريق.

---

تم إنشاء هذا الدليل بتاريخ: 2026-01-22
